#!/bin/bash

#use command:
#chmod +x start.sh && ./start.sh
#need to run setup first in the same folder before running this command

#need to change depending on path
python3 /home/pi/Tanks/src/mainprogram.py